# Domain docs

本仓库使用单上下文领域文档结构。

## Before exploring

- 阅读根目录 `CONTEXT.md` 中的统一术语。
- 阅读 `docs/adr/` 中与当前改动相关的架构决定。
- 文件不存在时继续工作，不需要预先创建。

## Rules

- ticket、规格、代码和测试使用 `CONTEXT.md` 中的 canonical terms。
- 不使用词汇表 `_Avoid_` 中列出的同义词。
- 输出与 ADR 冲突时必须明确报告，不得静默覆盖。
- 新术语和新架构决定通过 domain-modeling 工作流即时记录。
